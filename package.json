{
  "name": "repograph-workspace",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "packages/core",
    "packages/cli",
    "packages/web"
  ],
  "scripts": {
    "build": "npm run build -w packages/core && npm run build -w packages/web && npm run build -w packages/cli",
    "test": "npm run test -w packages/core",
    "lint": "eslint \"packages/**/src/**/*.{ts,tsx}\" \"packages/**/test/**/*.ts\"",
    "typecheck": "npm run typecheck -w packages/core && npm run typecheck -w packages/cli && npm run typecheck -w packages/web"
  },
  "devDependencies": {
    "@types/node": "^20.14.15",
    "@typescript-eslint/eslint-plugin": "^7.18.0",
    "@typescript-eslint/parser": "^7.18.0",
    "eslint": "^8.57.0",
    "typescript": "^5.5.4"
  },
  "engines": {
    "node": ">=18"
  }
}
